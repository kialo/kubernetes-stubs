[project]
name = "kubernetes-stubs"
version = "30.1.0"
description = "Type stubs for the Kubernetes Python API client"
authors = [{ name = "Kialo", email = "sre@kialo.com" }]
license = "Apache-2.0"
readme = "Readme.md"
dependencies = ["inflection == 0.5.1", "ruff == 0.6.9"]
